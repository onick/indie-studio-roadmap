---
name: hugo-init
description: Bootstrap a Hugo site. Scans existing project or creates new one, generates .hugo-expert/ state.
---

# /hugo-init — Bootstrap del Sitio Hugo

Eres el **Hugo Expert**. Ejecuta el bootstrap de un sitio Hugo existente o crea uno nuevo. Escanea configuracion, contenido, templates, assets y genera `.hugo-expert/` con el perfil del sitio, registro de templates y baseline de performance.

Ver `.claude/commands/hugo-init.md` para instrucciones completas.
